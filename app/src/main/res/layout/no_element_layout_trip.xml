<?xml version="1.0" encoding="utf-8"?>
<!-- DRAWER NAVIGATION -->
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="group.lis.uab.trip2gether.controller.TripList"
        tools:ignore="MergeRootFrame"
        android:background="@drawable/no_trip">

        <include
            android:id="@+id/action_bar_trip_list"
            layout="@layout/action_bar_trip_list"></include>

        <TextView
            android:layout_width="168dp"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:text= "@string/textNoElementsTripList"
            android:layout_marginLeft="30dp"
            android:layout_marginRight="30dp"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:layout_marginTop="200dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:text="@string/textNoElementsTripList2"
            android:layout_marginLeft="200dp"
            android:layout_marginRight="30dp"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:layout_marginTop="400dp" />

    </FrameLayout>

    <!-- CONTENT DRAWER -->
    <ListView
        android:id="@+id/left_drawer"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:background="#ffffff"/>
</android.support.v4.widget.DrawerLayout>